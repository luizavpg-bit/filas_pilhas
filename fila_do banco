package do_while;

import java.util.LinkedList;
import java.util.Queue;
import java.util.Scanner;

public class fila_banco {

    public static void main(String[] args) {
        Scanner leia = new Scanner(System.in);
        Queue<String> fila = new LinkedList<String>();
        int menu;

        do {
            System.out.println("1 - Adicionar Cliente");
            System.out.println("2 - Listar");
            System.out.println("3 - Retirar");
            System.out.println("4 - Sair");
            System.out.print("Digite a opção desejada ");
            menu = leia.nextInt();
            leia.nextLine(); 

            if (menu == 1) {
                System.out.print("Digite o nome aqui: ");
                String nome = leia.nextLine();
                fila.add(nome);
                System.out.println("Cliente adicionado: " + fila);
            } 
            
            else if (menu == 2) {
                if (fila.isEmpty()) {
                    System.out.println("Não há clientes na fila");
                } else {
                    System.out.println("Clientes na fila: " + fila);
                }
            } 
            
            else if (menu == 3) {
                if (fila.isEmpty()) {
                    System.out.println("Não há clientes na fila!");
                } else {
                    fila.poll();
                    System.out.println("Cliente chamado. Aguarde seu atendimento: " + fila);
                }
            }

        } while (opcao != 0);

        leia.close();
        
    }
}
